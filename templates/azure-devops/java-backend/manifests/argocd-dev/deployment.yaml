apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${{ values.component_id }}-dev
  namespace: alex-gitops-apps
  labels:
    app: ${{ values.component_id }}-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${{ values.component_id }}-dev
  template:
    metadata:
      labels:
       app: ${{ values.component_id }}-dev
    spec:
      containers:
        - name: container
          image: quay.io/alexmalo/devhub-demo:${{ values.component_id }}-dev
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP          
          imagePullPolicy: Always
          resources:
            limits:
              cpu: 100m
              memory: 128Mi
            requests:
              cpu: 50m
              memory: 128Mi
